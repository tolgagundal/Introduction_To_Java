package Chapter03;

import java.util.Scanner;

public class Ch03_28 {

	public static void main(String[] args) {

		// (Geometry: two rectangles)

		Scanner input = new Scanner(System.in);

		System.out.println("Enter r1's center x-, y-coordinates, width, and height: " + "r2 is inside r1  ");

		double r1x = input.nextDouble();
		double r1y = input.nextDouble();
		double r1w = input.nextDouble();
		double r1h = input.nextDouble();

		System.out.println("Enter r2's center x-, y-coordinates, width, and height: ");
		
		double r2x = input.nextDouble();
		double r2y = input.nextDouble();
		double r2w = input.nextDouble();
		double r2h = input.nextDouble();
		
		// Determine whether the second rectangle is inside the first
		
	}
}
		
		